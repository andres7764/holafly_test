const db = require('./db');
const swapiFunctions = require('./swapiFunctions');
const helpers = require('./helpers');
const people = require('./People');

module.exports = {
    db,
    swapiFunctions,
    helpers,
    people
}